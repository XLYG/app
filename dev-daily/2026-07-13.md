# 2026-07-13 开发记录

## 今日目标

建立项目开发规范和推进节奏，不编写业务功能代码。

## 已完成

- 创建 `docs/` 项目文档目录。
- 创建 `dev-daily/` 每日开发记录目录。
- 规划 MVP 范围和暂不开发事项。
- 规划小步推进的开发步骤。
- 建立技术规范、设计规范和执行标准。
- 更新 `CLAUDE.md`，加入文档路径、每日记录规则和工作说明。
- 完成第 1 步：整理最小目录结构。
- 将 `src/app/_layout.tsx` 从模板 Tab 结构改为简单 Stack 结构。
- 将 `src/app/index.tsx` 改为 AI 学习助手聊天页占位入口。
- 新增 `src/app/settings.tsx` 作为 API 配置页占位入口。
- 删除模板示例路由 `src/app/explore.tsx`。
- 新增 `src/services/`、`src/storage/`、`src/types/`、`src/utils/` 目录说明文件。
- 初始化 Expo lint 配置，新增 `eslint.config.js`，并安装 `eslint`、`eslint-config-expo` 开发依赖。
- 修复模板遗留的 web 颜色 hook lint 问题。
- 新增 CSS 类型声明，保证 TypeScript 检查通过。
- 根据新需求更新文档：存储优先采用后端连接 MySQL，DeepSeek API Key 由用户提交后保存。
- 新增 `docs/BACKEND_STORAGE_STANDARD.md`，明确前端不直连 MySQL、不直连 DeepSeek。
- 完成第 2 步：聊天页和 API 配置页静态界面。
- 聊天页新增消息展示区域、配置入口、输入框和发送按钮。
- 配置页新增用户名、DeepSeek API Key、保存配置和删除 Key 的静态表单。
- 删除已无用的模板 Tab 组件，避免继续引用已删除的 `explore` 路由。
- 开始第 3 步：确认后端与存储方案。
- Windows 侧确认 MySQL 客户端存在，`localhost:3306` 可连接。
- 新增 `server/` 后端目录，采用 Express + MySQL2 + dotenv。
- 新增 `server/.env.example`，只放环境变量示例，不包含真实密码或 Key。
- 新增 `server/schema.sql`，定义 `ai_study_assistant` 数据库和 `user_api_keys` 表。
- 新增 `GET /api/health` 和 `GET /api/db/health` 健康检查接口。
- 更新 `.gitignore`，忽略 `server/.env`。
- 完成第 4 步：保存配置和删除 Key。
- 新增 `server/crypto.js`，使用本地 `APP_SECRET` 对 DeepSeek API Key 加密后再存入 MySQL。
- 新增 `POST /api/config` 保存用户名和 DeepSeek API Key。
- 新增 `GET /api/config/:username` 查询用户是否已保存 Key，不返回 Key 明文。
- 新增 `DELETE /api/config/:username/key` 删除已保存 Key。
- 配置页接入后端接口，支持保存和删除操作。
- 安装 `playwright` skill，用于后续重启 Codex 后进行浏览器截图和视觉检查。
- 完成第 5 步：新增 `POST /api/chat`，后端读取 MySQL 中的 DeepSeek Key 后调用 DeepSeek。
- DeepSeek 默认模型配置为 `deepseek-v4-flash`。
- 完成第 6 步：聊天页接入真实提问、加载状态和失败状态。
- 优化聊天页和配置页视觉：统一浅灰背景、白色面板、状态条、清晰按钮和更紧凑的输入区。
- 根据用户反馈继续优化前端为移动端 Codex 风格：白底、极简顶部栏、自然消息流、黑色主按钮。
- 新增 `EXPO_PUBLIC_API_BASE_URL` 支持，方便手机真机访问电脑后端。
- 新增 `.env.example`，说明前端 API 地址配置。
- 新增 `docs/MOBILE_TESTING.md`，记录 Expo Go 真机测试步骤。
- 调整首页流程：用户进入后先输入用户名，后端确认该用户名已保存 Key 后才进入聊天。
- 未保存 Key 的用户名会显示配置提示，并跳转到配置页。
- 首页跳转配置页时会携带用户名，配置页自动填入。
- 完成第 7 步：基础验收和清理。
- 删除未使用的 Expo 模板组件、Tab 组件和示例图片资源。
- 移除未使用的模板依赖，保留当前 App 和后端实际需要的依赖。
- 重写 `README.md`，替换 Expo 模板说明为 AI 学习助手项目说明。
- 重启 Expo Web 服务，恢复 `http://localhost:8081` 页面访问。
- 更新 `docs/DEVELOPMENT_PLAN.md` 当前状态，标记第 0 步到第 7 步已完成。
- 开始前端体验调整第一轮。
- 首页调整为移动端对话应用结构：顶部小栏、中部欢迎语、底部用户名输入 Dock。
- 聊天页调整为顶部状态栏、自然消息流、底部问题输入 Dock。
- 配置页同步为更克制的移动端顶栏样式。

## 待办事项

- 重启 Codex 后使用 `playwright` skill 做页面截图检查。
- 使用用户真实测试 Key 从页面手动保存后，验证 DeepSeek 成功回复。
- 开始前端体验调整：围绕移动端 Codex 风格继续优化入口页、配置页和聊天页。
- 手机真机测试时，将 `.env.local` 的 `EXPO_PUBLIC_API_BASE_URL` 改成电脑局域网 IP。

## 验证记录

- 第 0 步只新增文档和协作说明，未运行 App。
- 已执行 `npm run lint`，通过。
- 已执行 `npx tsc --noEmit`，通过。
- 已执行 `npm run web -- --port 8081`，Expo Router 识别 `src/app` 为根目录，Web bundle 完成。
- Windows 侧访问 `http://localhost:8081` 返回 200。
- WSL 侧 `curl http://localhost:8081` 未连通，判断为当前 Windows/WSL 网络侧访问差异；Windows 侧验证可用。
- 第 2 步完成后再次执行 `npm run lint`，通过。
- 第 2 步完成后再次执行 `npx tsc --noEmit`，通过。
- Expo Web 热更新完成，Windows 侧访问 `http://localhost:8081` 返回 200。
- WSL 侧未发现 `mysql` 客户端，且 `127.0.0.1:3306` 未连通。
- Windows 侧发现 MySQL 客户端，`127.0.0.1:3306` 连通。
- 已执行 `npm run server:dev`，后端启动在 `http://localhost:3001`。
- Windows 侧访问 `GET /api/health` 返回成功。
- 未创建包含真实密码的 `server/.env`，所以 `GET /api/db/health` 返回 503 友好错误。
- 后端骨架完成后执行 `npm run lint`，通过。
- 后端骨架完成后执行 `npx tsc --noEmit`，通过。
- 已检查项目文件，未发现测试 DeepSeek Key 或数据库密码落盘。
- 已创建本地忽略文件 `server/.env`，用于后端读取 MySQL 密码和本地加密密钥。
- 已执行 `server/schema.sql`，MySQL 数据库和 `user_api_keys` 表初始化成功。
- 后端重启后 `GET /api/db/health` 返回成功。
- 使用占位测试 Key 验证 `POST /api/config` 保存成功。
- 使用占位测试用户验证 `GET /api/config/:username` 查询成功。
- 使用占位测试用户验证 `DELETE /api/config/:username/key` 删除成功。
- 删除后再次查询，`hasApiKey` 返回 `false`。
- 第 4 步完成后执行 `npm run lint`，通过。
- 第 4 步完成后执行 `npx tsc --noEmit`，通过。
- Windows 侧访问 `http://localhost:8081/settings` 返回 200。
- 第 5 和第 6 步完成后执行 `npm run lint`，通过。
- 第 5 和第 6 步完成后执行 `npx tsc --noEmit`，通过。
- 后端 `GET /api/health` 和 `GET /api/db/health` 均返回成功。
- 未配置用户请求 `POST /api/chat` 返回 400。
- 使用占位 Key 请求 `POST /api/chat` 返回 502，后端未崩溃。
- Windows 侧访问 `http://localhost:8081/` 和 `http://localhost:8081/settings` 均返回 200。
- UI 二次优化后执行 `npm run lint`，通过。
- UI 二次优化后执行 `npx tsc --noEmit`，通过。
- UI 二次优化后扫描项目文件，未发现测试 Key 或数据库密码落盘。
- 用户名入口流程调整后执行 `npm run lint`，通过。
- 用户名入口流程调整后执行 `npx tsc --noEmit`，通过。
- 验证 `GET /api/config/non-existing-user` 返回 `hasApiKey: false`。
- 验证首页和带用户名参数的配置页均返回 200。
- 第 7 步完成后执行 `npm run lint`，通过。
- 第 7 步完成后执行 `npx tsc --noEmit`，通过。
- 第 7 步完成后后端 `GET /api/health` 和 `GET /api/db/health` 均成功。
- 第 7 步完成后重启 Expo Web，首页和配置页均返回 200。
- 第 7 步完成后扫描项目文件，未发现测试 Key 或数据库密码落盘。
- 第 7 步完成后扫描源码和 `package.json`，未发现已删除模板组件和依赖的残留引用。
- 前端体验调整第一轮后执行 `npm run lint`，通过。
- 前端体验调整第一轮后执行 `npx tsc --noEmit`，通过。
- 前端体验调整第一轮后首页和配置页均返回 200。

## 风险与备注

- MVP 主流程已实现：用户名检查、Key 配置、Key 删除、MySQL 存储、DeepSeek 聊天请求。
- `package-lock.json` 变更较大，原因是首次运行 `expo lint` 自动安装 ESLint 相关开发依赖。
- 数据库密码和 DeepSeek API Key 不写入源码、文档或日志；后续通过环境变量或用户输入处理。
- 本次已确认 Windows 侧 MySQL 端口和账号级查询可用。
- 用户提供的 DeepSeek 测试 Key 未写入项目文件；配置页现在可以手动保存 Key。
- 本轮没有通过命令行使用用户真实测试 Key，避免进入命令历史或工具输出。
- `playwright` skill 已安装，但需要重启 Codex 后才能作为 skill 使用。
